"""
pynatem.modelos — pacote reservado (placeholder).

Este diretório é um placeholder para uma versão futura do pynatem. Ele está
reservado para abrigar, em um eventual desmembramento de ``blocos.py``, as
dataclasses de modelos de máquina síncrona (variantes MD01/MD02/MD03 do
``BlocoDMDG``) — hoje definidas em :mod:`pynatem.blocos`.

Nenhum símbolo é exportado nesta versão. O pacote é mantido intencionalmente
vazio para preservar a estrutura de diretórios prevista, sem alterar a
localização atual das classes. Ver ``CHANGELOG.md`` (v0.9.1) para a decisão
de organização correspondente.
"""
