coverage:
  precision: 2
  round: down
  range: "70...100"

ignore:
  - "tests"
  - "setup.py"

comment:
  layout: "reach,diff,flags,tree"
  behavior: default
  require_changes: false
  require_base: no
  require_head: yes
  hide_project_coverage: false
